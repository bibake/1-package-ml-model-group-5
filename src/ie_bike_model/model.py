import pickle

def train_and_persist(model_path = None):
	"""
	Train RandomForestRegressor and save
	"""
	
	print('To do')


def predict(parameters, model_path = None):
	"""
	Receives dictionary of input parameters, processes the input data,
	passes the data onto the trained model, and returns the number of
	expected users.
	"""
	
	# call train_and_presist if a pickled model isn''t found in working directory
	# return model.predict(parameters)
	
	print('To do')
